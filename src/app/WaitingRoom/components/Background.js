import styled from "styled-components";

const Background = styled.img`
    position: absolute;
    top: 0;
    margin-left: auto;
    margin-right: auto;
    height: 85vh;
    z-index: -4;
    object-fit: cover;
`;

export default Background;