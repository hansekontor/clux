import styled from "styled-components";

const List = styled.ol`
    align-items: flex-start;
    display: inline-flex;
    flex-direction: column;
    height: fit-content;
    position: relative;
    width: 90%;
    gap: 10px;
`;

export default List;