import styled from "styled-components";
import PlayerNumbersDefault from '@components/PlayerNumbers';

const PlayerNumbers = styled(PlayerNumbersDefault)`
    z-index: 1;
`;

export default PlayerNumbers;